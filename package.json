{
  "name": "moneymattersdaily",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-pinterest-feed.mjs",
    "build": "astro build",
    "postbuild": "node scripts/generate-sitemap.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "pin:generate": "node scripts/generate-pin.mjs",
    "pin:generate:all": "node scripts/generate-pin.mjs --all",
    "pinterest:feed": "node scripts/generate-pinterest-feed.mjs",
    "x:drafts": "node scripts/generate-x-drafts.mjs --all"
  },
  "dependencies": {
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "gray-matter": "^4.0.3"
  }
}
